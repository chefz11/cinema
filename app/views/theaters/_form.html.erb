<%= form_with(model: theater, local: true) do |form| %>
  <% if theater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(theater.errors.count, "error") %> prohibited this theater from being saved:</h2>

      <ul>
        <% theater.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <div class="field">
    <%= form.label :cleaned %>
    <%= form.check_box :cleaned %>
  </div>

  <div class="field">
    <%= form.label :movie %>
    <%= form.text_field :movie %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
